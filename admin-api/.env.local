# Admin API 本地開發環境變數範例
# 複製此文件為 .env.local 並修改配置

# 注意：本地開發時，MQTT 和 PostgreSQL 使用 localhost（因為它們在 Docker 中運行）
LOG_LEVEL=DEBUG
MQTT_BROKER=localhost
MQTT_PORT=1883
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=pump_testing
POSTGRES_USER=pump_user
POSTGRES_PASSWORD=pump_password_change_me
