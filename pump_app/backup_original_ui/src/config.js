// MQTT 配置
const config = {
  mqttHost: 'ws://localhost:8083/mqtt',  // MQTT broker 地址
  mqttUsername: '',                       // MQTT 用戶名（如需要）
  mqttPassword: '',                       // MQTT 密碼（如需要）
};

export default config;
